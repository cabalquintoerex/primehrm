# LGU PRIME-HRM — production server env  →  copy to  server/.env  on the box
# ---------------------------------------------------------------------------
# NEVER commit the real .env. Generate fresh secrets on the server:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ---------------------------------------------------------------------------

# MySQL — the dedicated database/user created in deploy/create-db.sql.
# URL-encode any special characters in the password.
DATABASE_URL="mysql://llcprime:CHANGE_ME_STRONG_PASSWORD@localhost:3306/llcprime"

# Auth secrets — REPLACE both with freshly generated 64-char hex strings.
JWT_SECRET=REPLACE_WITH_64_HEX_CHARS
JWT_REFRESH_SECRET=REPLACE_WITH_A_DIFFERENT_64_HEX_CHARS
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Backend port — must match the nginx proxy_pass (5010) and be free on the box.
PORT=5010

NODE_ENV=production

# The browser origin the app is served from. Same-origin (frontend + proxied API
# both under apps.cebu.gov.ph), so this is the scheme+host only — no /llcprime path.
# This box serves over plain HTTP (no SSL vhost). Use http:// here; switch to https
# once TLS is added. Must match the origin the browser actually uses.
CORS_ORIGIN=http://apps.cebu.gov.ph

# Scope auth cookies to this app's sub-path so they aren't sent to sibling apps
# on apps.cebu.gov.ph. Must match the deploy URL segment.
COOKIE_PATH=/llcprime

# Cookie Secure flag. MUST be false over plain HTTP or the browser drops the auth
# cookies and sessions silently expire. Set to true only after the site has HTTPS.
COOKIE_SECURE=false
